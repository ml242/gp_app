<br><br>
<%= link_to 'Add a New Place', new_item_path %><br><br>
<br><br>
<%= link_to 'Edit Item', edit_item_path %> | <%= link_to 'Delete Item', @item, :method => :delete, :confirm => "Are you sure?" %>

<h3><%= @item.name %></h1>
<img src="<%= @item.img_url %>" width="25%"><br>
Description: <%= @item.description %><br>

<!-- <button>Add <%= @item.name %> to:</button>
<input type="select" name="add-to-itinerary" options_for_select=""> -->

<%# itineraries_array = Itinerary.all.map { |itinerary| [itinerary.title, itinerary.id] %>

<%#= form_for([@itinerary]) do |f| %>
<%#= f.label "To which itinerary do you want to add?" %>

<%#= f.select options_for_select(itineraries_array) %>
<%#= f.submit %>

<%= options_form_collection_for_select(Itinerary.all, :id, :title) %>