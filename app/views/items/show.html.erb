<%= link_to 'Items', items_path %> | <%= link_to 'Itineraries', itineraries_path %><br><br>
<%= link_to 'Add', new_item_path %><br><br>
<br><br>
<%= link_to 'Edit Item', edit_item_path %> | <%= link_to 'Delete Item', @item, :method => :delete, :confirm => "Are you sure?" %>

<h3><%= @item.name %></h1>
<img src="<%= @item.img_url %>" width="25%"><br>
<%= @item.description %><br>
Parent-id: <%= @item.parent_id %>

<button>Add this item to itinerary</button>