<div class="titlez">
<h1 id="items-index-title">Items Index Page</h1>
</div>
<br>

<%= render 'slideshow' %>

<br>

<% @rand_items.each do |item| %>

  <h3><%= item.name %></h2><br>

  <%= item.description %><br>
  <a href="/items/<%= item.id %>"><img src="<%= item.img_url %>"></a><br>

<% end %>

<%= link_to 'Post a new item', new_item_path %>