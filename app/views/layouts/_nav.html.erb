<header class="container">

  <% if user_signed_in? %>

  <div class="one-col"></div>

  <div class="gp-logo">
    <a href="/itineraries">goin' places</a>
  </div>

  <div class="four-col"></div>
  <div class="nav-bar-right">

      Hello, <%= current_user.name %> | <a href="/users/<%= current_user.id %>">Dashboard</a> | <%= link_to('Travel Itineraries', itineraries_path, :method => :get) %> | <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>

  </div>
</div>

<% end %>

</header>
